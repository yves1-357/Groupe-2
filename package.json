{
  "name": "40e-de-bierges-fullstack",
  "version": "1.0.0",
  "description": "Site de la 40e de Bierges - Full Stack",
  "scripts": {
    "build": "cd api && npm install && cd ../40bierges && npm install && npm run build && cd .. && npm run postbuild",
    "postbuild": "rm -rf api/public && cp -r 40bierges/build api/public",
    "start": "cd api && npm start"
  },
  "engines": {
    "node": "16.x"
  }
}
